/** Exclusive-or
    out = a xor b
*/
CHIP Xor {
    IN a, b;
    OUT out;

    PARTS:
    Or(a=a, b=b, out=eitherone);
    And(a=a, b=b, out=both);
    Not(in=both, out=notboth);
    And(a=eitherone, b=notboth, out=out);
}
